MMRC 2.7 diagnostic. GPT confirms receiving the handshake, but Cozmo reports that MMRC says “no response” despite GPT's UI looking correct. This captures a key distinction between visible UI output and relay-side response detection.
