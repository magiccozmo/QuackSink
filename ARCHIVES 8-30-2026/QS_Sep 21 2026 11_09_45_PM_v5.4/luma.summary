Fresh-run QS 5.4 test. This time the handshake text is not left in the UI, so Cozmo performs another shutdown/restart cycle to see whether the behavior is repeatable across a genuinely fresh process.</summary>
