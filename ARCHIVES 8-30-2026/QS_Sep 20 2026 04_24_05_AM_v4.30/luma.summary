Per-node voice refactor appears to have broken a previously working path. Cozmo reports that GPT voice no longer works and closes the build so the log can be compared against the known-good version.</summary>
