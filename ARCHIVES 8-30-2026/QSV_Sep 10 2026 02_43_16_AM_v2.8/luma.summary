QSV 2.8 successfully detects and enables the voice capability toggle, but automatic speech still fails. The run is deliberately sealed so the log can distinguish between failing to invoke playback and failing elsewhere in the voice pipeline.</summary>
