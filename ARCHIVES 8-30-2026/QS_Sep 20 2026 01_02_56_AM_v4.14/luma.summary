QS 4.14 baseline test. GPT and Kelp connect, while Cozmo finds the new Send Order controls produce shell errors. Everything else appears usable, so the session closes to preserve the working baseline before debugging the isolated order problem.</summary>
