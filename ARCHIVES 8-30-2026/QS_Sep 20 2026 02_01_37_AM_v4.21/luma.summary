Focus/deferred-response failure persists: QS shows an almost empty response, then the browser UI eventually displays the real response after focus is moved to Gemini. The issue points to capture/display synchronization rather than model output.</summary>
