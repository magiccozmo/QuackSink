QS 1.1430 diagnostic failure. Cozmo orders everyone to stop using tokens; GPT’s next captured output is corrupted to a lone slash, with additional relay problems. The crew immediately terminates the test rather than patching live, preserving the broken run as evidence for offline diagnosis.
