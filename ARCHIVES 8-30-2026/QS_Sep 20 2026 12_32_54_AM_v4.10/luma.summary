UI-focused QS iteration. Cozmo explicitly asks for a larger output window and a simpler horizontal Send Order control with draggable buttons. The resulting 4.12 design changes the operator panel to a horizontal row of draggable numbered node chips, persists the order, compresses the surrounding UI, and gives the relay output substantially more vertical space while leaving browser/relay logic untouched.</summary>
