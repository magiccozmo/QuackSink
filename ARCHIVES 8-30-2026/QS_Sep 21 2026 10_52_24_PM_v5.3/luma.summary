QS 5.3 startup-performance test. The handshake works, but Cozmo notices that importing/initializing Kokoro makes startup substantially slower. The session is closed specifically so the log can be used to measure where the startup time is going.</summary>
