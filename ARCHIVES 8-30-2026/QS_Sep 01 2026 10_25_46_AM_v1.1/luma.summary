Very small Grok/DOM-discovery reaction. Cozmo sees an odd handshake and then realizes that the new node-DOM discovery machinery is dramatically reducing the old pain of coding against each individual LLM UI.
